# Express server port (default: 3001)
PORT=3001

# Frontend API base URL
# In development the Vite proxy handles /api → http://localhost:3001
# In production point this at your deployed backend
VITE_API_URL=
